name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

# No protobuf compiler is installed anywhere in this file on purpose: the build
# script compiles the `.proto` with `protox`, a pure-Rust compiler, so a bare
# Rust toolchain is all any job needs.
#
# Every job runs on the organisation's self-hosted runner, selected by the
# label set below (all labels must match). The runner ships a Rust toolchain
# managed by rustup and Node.js (needed by the JavaScript-based actions), which
# is what `dtolnay/rust-toolchain` relies on to select or install the exact
# toolchain each job asks for.
jobs:
  fmt:
    name: Format
    runs-on: [self-hosted, Linux, X64, node, rust]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: [self-hosted, Linux, X64, node, rust]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --all-targets --all-features -- -D warnings

  test:
    name: Test
    runs-on: [self-hosted, Linux, X64, node, rust]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test

  docs:
    name: Docs
    runs-on: [self-hosted, Linux, X64, node, rust]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo doc --no-deps
        env:
          RUSTDOCFLAGS: -D warnings

  msrv:
    name: MSRV 1.88
    runs-on: [self-hosted, Linux, X64, node, rust]
    steps:
      - uses: actions/checkout@v4
      # Must match `rust-version` in Cargo.toml. `--locked` is what makes this
      # job meaningful: it checks the committed Cargo.lock, not whatever newer
      # resolution a fresh update would pick.
      - uses: dtolnay/rust-toolchain@1.88.0
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --lib --all-features --locked

  deny:
    name: cargo-deny
    runs-on: [self-hosted, Linux, X64, node, rust]
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
